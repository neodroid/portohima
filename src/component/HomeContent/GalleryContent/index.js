import React from "react";
import {GlobalContent} from "../../GlobalContent";
import {TitleHomeContent} from "../style"

const GalleryContent = () => {
    return(
        <GlobalContent>
            <TitleHomeContent>Recent Gallery</TitleHomeContent>
        </GlobalContent>
    );
};

export default GalleryContent;